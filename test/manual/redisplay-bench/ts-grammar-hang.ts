  options: {
  items: [
    "SomeItem({
            "lo rem'l orem lo remlore mlor emlo remlo remlore. mlorem lorem lore mlore ml oremlo r eml oremlor.",
          );
        if (storedRecord.secretVl) {
          const matchesValue1 = await compute(
            inputValues.secretVl,
            storedRecord.secretVl,
            throw new Error(
              'ips umipsumi psu mipsumi ps umipsumip. sumips umi psumi.',
            );
          }
        }
        return {
