# Evidence records

One Markdown file per run, named `YYYY-MM-DD-<os>-<mode>-<topic>.md`.

See the field template, scenario checklist, and what scripted checks do and
do not cover in [`../README.md`](../README.md).
